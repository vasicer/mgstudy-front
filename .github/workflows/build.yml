# ─────────────────────────────────────────────────────────────
# .github/workflows/build.yml  —  Front (Next.js) CI
#
# Trigger:
#   - pull_request → main : lint + test + docker build (push 없음)
#   - push → main         : lint + test + build + GHCR push
#
# Repository Variables (Settings → Secrets and variables → Variables):
#   STUDENT_NAME = alice    ← 반드시 설정!
#
# Org-level Secrets (자동 상속):
#   SERVER_SSH_HOST, SERVER_SSH_USER, SERVER_SSH_KEY, GHCR_DEPLOY_TOKEN
# ─────────────────────────────────────────────────────────────
name: Build & Push

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  statuses: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  REGISTRY: ghcr.io
  ORG: codingfit-academy

jobs:
  # ── Lint & Test ────────────────────────────────────────────
  lint-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: |
          if [ -f yarn.lock ]; then yarn --frozen-lockfile
          elif [ -f package-lock.json ]; then npm ci
          elif [ -f pnpm-lock.yaml ]; then corepack enable pnpm && pnpm i --frozen-lockfile
          else npm install
          fi

      - name: Lint
        run: npm run lint --if-present

      - name: Type check
        run: npm run type-check --if-present

      - name: Test
        run: npm test --if-present -- --passWithNoTests

  # ── Docker Build & Push ────────────────────────────────────
  docker-build:
    name: Docker Build${{ github.event_name != 'pull_request' && ' & Push' || '' }}
    runs-on: ubuntu-latest
    needs: lint-test
    outputs:
      sha_tag: ${{ steps.sha.outputs.tag }}
      image_digest: ${{ steps.build.outputs.digest }}

    steps:
      - uses: actions/checkout@v4

      - name: Short SHA
        id: sha
        run: echo "tag=sha-$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.ORG }}/${{ github.event.repository.name }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=stable,enable=${{ github.event_name != 'pull_request' }}
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=Academy Front for ${{ vars.STUDENT_NAME }}
            academy.student=${{ vars.STUDENT_NAME }}
            academy.service=front

      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          sbom: false

      - name: Summary
        if: github.event_name != 'pull_request'
        run: |
          {
            echo "## ✅ Build & Push 완료"
            echo "| 항목 | 값 |"
            echo "|---|---|"
            echo "| Student | \`${{ vars.STUDENT_NAME }}\` |"
            echo "| Image | \`${{ env.REGISTRY }}/${{ env.ORG }}/${{ github.event.repository.name }}\` |"
            echo "| SHA Tag | \`${{ steps.sha.outputs.tag }}\` |"
            echo "| Digest | \`${{ steps.build.outputs.digest }}\` |"
            echo ""
            echo "➡ 선생님이 어드민 콘솔에서 Deploy 버튼을 누르면 서버에 배포됩니다."
          } >> $GITHUB_STEP_SUMMARY
